# Keep the build context lean and keep secrets/junk out of the image.
# Without this, `COPY . .` would bake .git history and any local .env
# file into the image.
node_modules
dist
.git
.env
.env.local
*.log
